// Create wrapper component to render the actual dashboard from the (dashboard) route group
'use client';
import DashboardPage from '../(dashboard)/page';

export default function Page() {
  return <DashboardPage />;
}
